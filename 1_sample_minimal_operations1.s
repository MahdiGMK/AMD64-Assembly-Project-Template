global asm_main
section .text
asm_main:

    mov rax, 20
    mov rbx, 30
    add rax, rbx
    ;; 50

    ;amd64(X86-64) ASSEMBLY INSTRUCTION REFERENCE
    ;https://www.felixcloutier.com/x86/

    ret
